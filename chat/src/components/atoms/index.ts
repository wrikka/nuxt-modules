// Atomic components - smallest reusable UI elements
// This directory will contain basic UI components like buttons, inputs, etc.
// Currently empty, will be populated as atomic components are extracted

export {}
