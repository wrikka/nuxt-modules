import { defineNuxtPlugin } from "#app";

export default defineNuxtPlugin((_nuxtApp) => {
	// Client-side diff plugin
	// Add any client-side initialization here
	console.log("[Diff Module] Client plugin initialized");
});
