# Project Improvement Plan

Based on the user's request to apply `/follow-nuxt` and `/make-completely` workflows to the project at `d:\sheet\apps\web`.

1.  **Analyze Project**:
    -   Analyze `package.json` to understand dependencies and scripts.
    -   Analyze the overall project structure.

2.  **Follow Nuxt Best Practices (`/follow-nuxt`)**:
    -   Review and set up `nuxt.config.ts`.
    -   Refactor pages, components, composables, layouts, stores, middleware, plugins, and server routes according to Nuxt 3 conventions.
    -   Ensure all new and existing files adhere to the specified rules for each directory (`pages/`, `components/`, `composables/`, etc.).

3.  **Make Completely (`/make-completely`)**:
    -   Improve existing features.
    -   Enhance UX/UI.
    -   Optimize performance.
    -   Improve code quality by running linters and formatters.

4.  **Finalization**:
    -   Test the application to ensure everything works as expected.
    -   Run the linter one last time to catch any remaining issues.
